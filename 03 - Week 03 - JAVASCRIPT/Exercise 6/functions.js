// Create a function called greet that takes a name parameter
function greet(name) {
    // Make the greet function return "Hello, [name]!"
    return 'Hello, ' + name + '!';
}

// Create a function called add that takes two parameters
function add(num1, num2) {
    // Return the sum of num1 and num2
    return num1 + num2;
}

// Call the greet function and store the result in a variable
let greetingMessage = greet('Usisipho');
// console.log(greetingMessage); // Print the greeting message

// Call the add function and store the result in a variable
let sumResult = add(5, 10);
// console.log(sumResult); // Print the sum of both parameters

console.log(greetingMessage, sumResult); // Print both variables
// ^_+